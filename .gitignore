# Environment
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/

# Alembic generated migrations (keep versions under source control)
# — intentionally not ignored

# Pytest / coverage
.pytest_cache/
.coverage
htmlcov/

# Mypy / Ruff
.mypy_cache/
.ruff_cache/

# IDE
.idea/
.vscode/
*.swp
*.swo

# macOS
.DS_Store

# Playwright browser session (contains auth cookies — never commit)
.oegk_browser_session/
**/oegk_browser_session/

# Raw Merkur calibration captures (may contain PII — never commit)
# Only sanitized copies go in tests/fixtures/merkur/
scripts/.merkur_capture/

# Merkur browser session (contains OAuth cookies — never commit)
scripts/.merkur_session/
**/.merkur_browser_session/

# Local storage mount (not real files, just a mount point)
storage/

# Docker
*.tar

# Frontend
frontend/node_modules/
frontend/dist/
